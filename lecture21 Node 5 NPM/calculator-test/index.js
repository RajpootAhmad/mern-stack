const { add, subtract } = require("ahmad-calculator-2026-unique");
const _ = require("underscore");
console.log(add(19, 2));
const fs = require("fs"); //how we call built in modules
const os = require("os");
console.log(os.freemem());
const res = _.contains([1, 2, 3, 6, 8], 8);
console.log(res);
